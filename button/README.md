# Button Component

## Props

- `variant`: Estilo visual (default: 'default')
- `size`: Tamaño (default: 'default')
- `as`: Etiqueta HTML (default: 'button')
- `disabled`: Estado deshabilitado (default: false)
- `...attributes` (mergeable)

## Slots

- Contenido del botón

## Ejemplo de uso

```blade
<x-button variant="outline" size="sm">Guardar</x-button>
```

## Detalles

Botón estilizado, configurable por variante, tamaño y atributos.
